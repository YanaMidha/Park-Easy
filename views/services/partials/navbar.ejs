<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Navbar Example</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f5ebe0;
    }
    
    .navbar {
      background-color: black;
      color: white;
      padding: 10px 20px;
      height: 70px;
    }
    
    .nav-list {
      list-style: none;
      display: flex;
      align-items: center;
      justify-content: space-between;
      width: 100%;
    }

    .nav-list h3 {
      margin: 10px 0;
      text-align: center;
      font-family: 'Times New Roman', Times, serif;
      font-size: 30px;
      color: lightcyan;
      animation: textclip 2s linear infinite;
      flex-grow: 1; /* Allow the h3 to take available space */
    }
    
    .nav-list li {
      text-align: center; /* Align the cities to the center */
    }
    
    .nav-list li:first-child {
      margin-right: auto; /* Pushes "CITIES" text to the left */
    }
    
    .nav-buttons {
      display: flex;
      align-items: center;
    }
    
    .btn-faq,
    .btn-back {
      padding: 8px 15px;
      margin-left: 10px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s ease; /* Added hover transition */
    }
    
    .btn-faq:hover,
    .btn-back:hover {
      background-color: rgba(255, 255, 255, 0.1); /* Hover background color */
    }
    
    .btn-faq {
      background-color: #4285f4;
      color: white;
      height: 35px;
      width: 80px;
    }
    
    .btn-back {
      background-color: #ea4335;
      color: white;
      height: 35px;
      width: 80px;
    }
    
    .animate-character {
      text-transform: uppercase;
      background-image: linear-gradient(
        -225deg,
        skyblue 0%,
        lightcyan 29%,
        lightgreen 67%,
        lightyellow 100%
      );
      background-size: auto auto;
      background-clip: border-box;
      background-size: 200% auto;
      color: #fff;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent; /* Use -webkit-text-fill-color */
      animation: textclip 2s linear infinite;
      display: inline-block;
      font-size: 190px;
    }
    
    @keyframes textclip {
      to {
        background-position: 200% center;
      }
    }
    
  </style>
</head>
<body>
  <nav class="navbar">
    <ul class="nav-list">
      <li>
        <h3 class="animate-character">SERVICE PROVIDERS</h3>
      </li>
      <!-- Empty li for spacing -->
      <li></li>
      <li class="nav-buttons">
        <button class="btn-faq">FAQ</button>
        <button class="btn-back" onclick="fun1()">Back</button>
      </li>
    </ul>
  </nav>
  <script>
    function fun1(){
      window.location="/cities/<%=CityId%>";
    }
  </script>
</body>
</html>
